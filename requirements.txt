fastapi==0.100.0      # FastAPI framework for building the backend
uvicorn==0.23.0       # ASGI server to run the FastAPI application
pydantic==1.10.2      # Data validation and serialization
python-dotenv==1.0.0  # Load environment variables (optional, if used)
pandas==2.0.3         # For advanced CSV handling and data analysis (optional)
faiss-cpu
transformers
torch

